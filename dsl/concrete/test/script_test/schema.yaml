$schema: http://json-schema.org/draft-07/schema#
implements: test:unit
allOf:
- $ref: ../../base_concrete_step.yaml
- properties:
    type:
      const: test:script_test
    config:
      type: object
      required:
      - script
      properties:
        script:
          type: string

